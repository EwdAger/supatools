# Directory Structure

> Current structure expectations for `ztools-api-types`.

The package directory is currently empty in this repo checkout. Treat it as an output target, not as a local frontend implementation area.

Actual source of truth here:

- `scripts/sync-api-types.js`
- generated package files under `ztools-api-types/` when sync is run
