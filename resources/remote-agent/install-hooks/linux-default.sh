#!/bin/sh

# Default Linux install hook.
# Intentionally minimal: keep as a safe example that proves the hook mechanism
# works without requiring host-specific firewall or service changes.

echo "[ztools-agent hook] linux-default: no extra system changes applied"
